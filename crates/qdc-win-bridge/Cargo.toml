[package]
name = "qdc-win-bridge"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]
name = "qdc_engine"

[dependencies]
pdf-kernel = { path = "../pdf-kernel" }
memory-governor = { path = "../memory-governor" }
tile-engine = { path = "../tile-engine" }
gpu-compositor = { path = "../gpu-compositor" }
text-engine = { path = "../text-engine" }
annot-engine = { path = "../annot-engine" }
color-engine = { path = "../color-engine" }

uuid.workspace = true
log.workspace = true
env_logger.workspace = true
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
parking_lot.workspace = true
wgpu.workspace = true
pollster = "0.3.0"
raw-window-handle = "0.6"
